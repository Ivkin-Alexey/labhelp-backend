name: backend-deploy

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ main ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    - name: Deploy using ssh
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.PRIVATE_KEY }}
        port: 22
        script: |
          set -e  # Останавливаем выполнение при ошибке
          
          cd /home/git/labhelp-backend
          
          echo "📥 Получаем последние изменения..."
          git pull origin main
          
          echo "📦 Устанавливаем зависимости..."
          npm install
          
          # Проверяем подключение к базе данных
          echo "🔗 Проверяем подключение к базе данных..."
          npx prisma db execute --stdin <<< "SELECT 1;" || {
            echo "❌ Ошибка подключения к базе данных"
            exit 1
          }
          
          # Исправляем проблему с миграцией если она есть
          echo "🔧 Проверяем и исправляем проблемы с миграциями..."
          npx prisma db execute --file scripts/fix-migration-issue.sql || {
            echo "⚠️ Не удалось выполнить скрипт исправления, продолжаем с миграциями"
          }
          
          # Проверяем наличие новых миграций
          echo "🔍 Проверяем статус миграций..."
          MIGRATION_STATUS=$(npx prisma migrate status 2>&1 || true)
          echo "$MIGRATION_STATUS"
          
          # Выполняем миграции если они есть
          if echo "$MIGRATION_STATUS" | grep -q "Database schema is up to date"; then
            echo "✅ База данных актуальна, миграции не требуются"
          else
            echo "📦 Выполняем миграции базы данных..."
            npx prisma migrate deploy || {
              echo "❌ Ошибка выполнения миграций, пытаемся исправить..."
              npx prisma migrate resolve --applied 20250913150808_add_filter_tables || true
              npx prisma migrate deploy
            }
            echo "✅ Миграции выполнены успешно"
          fi
          
          # Проверяем и применяем индексы для оптимизации поиска (только если их нет)
          echo "🔍 Проверяем наличие индексов поиска..."
          INDEX_EXISTS=$(npx prisma db execute --stdin <<< "SELECT 1 FROM pg_indexes WHERE indexname = 'idx_equipment_name_gin';" 2>/dev/null | grep -c "1" || echo "0")
          if [ "$INDEX_EXISTS" -eq "0" ]; then
            echo "📦 Применяем индексы для оптимизации поиска..."
            npx prisma db execute --file scripts/apply_search_indexes.sql || {
              echo "⚠️ Не удалось применить индексы поиска, продолжаем без них"
            }
            echo "✅ Индексы поиска применены"
          else
            echo "✅ Индексы поиска уже существуют"
          fi
          
          # Генерируем клиент Prisma
          echo "🔄 Генерируем клиент Prisma..."
          npx prisma generate
          echo "✅ Клиент Prisma сгенерирован"
          
          # Перезапускаем приложение
          echo "🚀 Перезапускаем приложение..."
          pm2 restart index
          
          # Проверяем статус приложения
          echo "🔍 Проверяем статус приложения..."
          pm2 status
          
          echo "✅ Деплой завершен успешно!"